# $schema: https://www.schemastore.org/sqlc-2.0.json
version: "2"
sql:
    - engine: sqlite
      schema: "db/migrations"
      queries: "db/queries"
      gen:
          go:
              package: "queries"
              out: "db/queries"
              emit_json_tags: true
