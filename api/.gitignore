*.sqlite
*/_artifacts
.task/

# Compiled binary output
/bin/

# Embedded frontend build output (generated by `task web:build`).
# Keep the directory tracked via .gitkeep so `//go:embed all:dist` always compiles.
/static/dist/*
!/static/dist/.gitkeep
